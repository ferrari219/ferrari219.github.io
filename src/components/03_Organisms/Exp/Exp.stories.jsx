import Exp from './Exp';

export default {
	title: '03_Organisms/Exp',
	component: Exp,
};

const Template = (args) => <Exp {...args} />;

export const Overview = Template.bind({});
Overview.args = {};
